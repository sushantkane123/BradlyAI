# ── Core ──
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
sqlalchemy>=2.0.28
pydantic>=2.6.4
pydantic-settings>=2.2.1
websockets>=12.0
python-multipart>=0.0.9
rich>=13.7.1

# ── Database drivers ──
aiosqlite>=0.20.0           # SQLite (default, dev/demo)
asyncpg>=0.29.0             # Postgres (production)
psycopg2-binary>=2.9.9      # Postgres (sync fallback)

# ── HTTP / Async ──
httpx>=0.27.0
requests>=2.31.0
slowapi>=0.1.9              # rate limiting

# ── Auth / Security ──
PyJWT>=2.8.0                # JWT signing/verification
bcrypt>=4.0.0               # password hashing (used directly by services/password.py)
python-multipart>=0.0.9     # OAuth2 password flow form parsing
pyotp>=2.9.0                # MFA TOTP
qrcode[pil]>=7.4.2          # MFA QR setup
authlib>=1.3.0              # OAuth2 / OIDC / SAML

# ── Sigma / playbooks ──
pyyaml>=6.0.1
jinja2>=3.1.3

# ── Observability ──
prometheus-client>=0.20.0

# ── Reporting ──
reportlab>=4.1.0            # PDF reports

# ── Testing ──
pytest>=8.1.1
